/*#pragma once

#ifdef _DEBUG

#include <new>

void* operator new(size_t blocksize) throw(std::bad_alloc);
void* operator new[](size_t blocksize) throw(std::bad_alloc);
void  operator delete(void* block) throw();
void  operator delete[](void* block) throw();

#endif*/
